import 'package:flutter/material.dart';

abstract class AppColors {
  const AppColors._();
  static const Color primaryColor = Colors.orange; // Orange color
  static const Color whiteColor = Colors.white; // White color
}
